import { ListAnchorDirective } from './list-anchor.directive';

describe('ListAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ListAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
