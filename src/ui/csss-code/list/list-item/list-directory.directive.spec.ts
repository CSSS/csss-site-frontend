import { ListDirectoryDirective } from './list-directory.directive';

describe('ListDirectoryDirective', () => {
  it('should create an instance', () => {
    const directive = new ListDirectoryDirective();
    expect(directive).toBeTruthy();
  });
});
